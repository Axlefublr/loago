Consistent ordering of tasks both in the data file and in the output of subcommands. \
Data directory `~/.local/share/loago/` and the data file `~/.local/share/loago/loago.json` are now only created once there is data to save, rather than ensured to exist whenever you first run the program for the first time.
